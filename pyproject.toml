[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pitwall-analytics"
version = "0.1.0"
description = "Pitwall Analytics – motorsport analytics dashboard"
authors = [ { name = "Your Name" } ]
dependencies = [
  "dash>=2.9",
  "pandas>=2.0",
  "fastf1>=3.0",
  "plotly>=5.0",
]
